PS A:\AptWeb\zypper-operations\Archive-Hasher\cmd\archive-hasher> go run main.go --help
Usage of C:\Users\ADMINI~1\AppData\Local\Temp\go-build3651438502\b001\exe\main.exe:
  -format string
        Snapshot format override (default: derived from extension)
  -output-basename string
        Base filename without extension for generated outputs (default "snapshot-hashes")
  -output-dir string
        Directory for generated outputs (default: <artifact>.aamhs beside the artifact)
  -overwrite
        Overwrite existing outputs if present
  -snapshot-datetime string
        UTC datetime in RFC3339 format (default: artifact modified time)
  -snapshot-name string
        Friendly snapshot name (default: derived from artifact name)
  -verbose
        Print progress information
		
go run main.go -output-dir "D:\" -verbose "D:\Checked.zip"






Usage of C:\Users\ADMINI~1\AppData\Local\Temp\go-build2770085841\b001\exe\main.exe:
  -key string
        Path to an ASCII-armored PGP private key
  -openssl string
        OpenSSL command path for PQ signing (default "openssl")
  -output string
        Output path for the detached ASCII-armored signature (default: <manifest>.asc)
  -overwrite
        Overwrite existing detached signatures if present
  -passphrase-env string
        Environment variable that stores the PGP private key passphrase
  -pq-algorithm string
        PQ signature algorithm (default "SLH-DSA-SHAKE-256s")
  -pq-key string
        Path to an OpenSSL SLH-DSA private key for the PQ detached signature
  -pq-output string
        Output path for the armored PQ signature envelope (default: <manifest>.sphincs)
  -verbose
        Print progress information
		
		
		
		
		
./manifest-signer -key "B:\zypper-operations\Archive-Hasher\AptlantisSigningKey-Private.asc" -overwrite -verbose "A:\downloads\python\snapshot-hashes.txt"

openssl genpkey -algorithm SLH-DSA-SHAKE-256s -out sphincs.key
openssl pkey -in sphincs.key -pubout -out sphincs.pub
./manifest-signer -key "B:\zypper-operations\Archive-Hasher\AptlantisSigningKey-Private.asc" -pq-key ".\sphincs.key" -overwrite -verbose "A:\downloads\python\snapshot-hashes.txt"
